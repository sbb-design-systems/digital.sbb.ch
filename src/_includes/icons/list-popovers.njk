{% for icon in icons %}
{% if (icon.type == mode) %}
<sbb-popover id="{{ icon.name }}-trigger" trigger="{{ icon.name }}" hover-trigger>
    <div class="icon-content" id="{{ icon.name }}-content">
        <sbb-title level="5">{{ icon.name }}</sbb-title>
        <p class="sbb-text-s popover-max-width">
            {{ translations[page.lang].iconcategory }}: {{ icon.category }}<br>
            {% if mode == "Pictograms" %}
            {% else %}
            {{ translations[page.lang].icontags }}: {{ icon.tags }}
            {% endif %}
        </p>
        {% if mode == "KOM" %}
            <div class="icon-items">
                {% for size in icon.sizes %}
                <div class="icon-item">
                    <div class="icon-preview">
                    <img loading="lazy" class="icon-png size-{{size}}" src="https://icons.app.sbb.ch/icons/{{icon.group}}-{{size}}.svg" alt="{{image.group}}-{{size}}" />
                    </div>
                    <p class="sbb-text-s">{{size}}</p>
                </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
</sbb-popover>
{% endif %}
{% endfor %}